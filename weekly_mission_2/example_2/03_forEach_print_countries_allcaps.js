const countries = ['Finland', 'Denmark', 'Sweden', 'Norway', 'Iceland'];
console.log('Example 3: Using forEach to print out the elements on a list in all caps');
countries.forEach(country => console.log(country.toUpperCase()));